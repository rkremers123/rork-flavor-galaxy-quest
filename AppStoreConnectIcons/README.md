# App Store Connect icon sizes

Pre-sized from the locked teal Saturn-ring App Icon (navy). In-app `AppIcon.appiconset` stays the single universal 1024.

Push marks (`push-*.png`) are centered Saturn on navy for notification small icons.

Do not redesign. Not Flavor Galaxy.
